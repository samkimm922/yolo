{
  "schema_version": "1.0",
  "schema": "yolo.demand.evidence_result.v1",
  "role": "cross-checker",
  "status": "completed",
  "completed": true,
  "claim": "CONFIRMED: YOLO demand evidence schema, prompt, readiness contract, and tests distinguish and enforce project, external, user, and unknown evidence scopes. Project facts require project-scoped evidence; external research is allowed only as background.",
  "confidence": "high",
  "evidence": [
    {
      "path": "src/demand/router.ts",
      "line": "44",
      "scope": "project",
      "source": "project_code",
      "summary": "DEMAND_EVIDENCE_RESULT_SCHEMA_DEFINITION fields.evidence explicitly states: 'scope is project | external | user | unknown; project facts require project-scoped evidence from code, tests, docs, config, logs, or artifacts.'",
      "why": "This is the authoritative schema definition that establishes scope requirements."
    },
    {
      "path": "src/demand/router.ts",
      "line": "380-402",
      "scope": "project",
      "source": "project_code",
      "summary": "evidenceRecordScope() normalizes scope field to four canonical values (project, external, user, unknown) and maps source types: project_code, project_test, project_docs, project_config, project_log, project_artifact → project; external_web, external_docs → external; user → user.",
      "why": "This function enforces the four-way scope classification at runtime."
    },
    {
      "path": "src/demand/router.ts",
      "line": "480-490",
      "scope": "project",
      "source": "project_code",
      "summary": "projectEvidenceBlockingIssues() returns [] when summary.has_project is true, otherwise returns blocker with code PROJECT_FACT_REQUIRES_PROJECT_EVIDENCE.",
      "why": "This is the blocking logic that prevents non-project evidence from satisfying existing-project facts."
    },
    {
      "path": "src/demand/evidence-dispatch.ts",
      "line": "266-269",
      "scope": "project",
      "source": "project_code",
      "summary": "buildDemandEvidenceAgentPrompt() hard rules include: 'Every evidence record must include scope: project, external, user, or unknown.' and 'Project facts require project-scoped evidence from code, tests, docs, config, logs, or artifacts.' and line 269 instructs to mark external research as scope=external and not use as project facts.",
      "why": "This is the agent protocol that enforces scope separation during evidence gathering."
    },
    {
      "path": "__tests__/demand-router.test.ts",
      "line": "340-385",
      "scope": "project",
      "source": "project_test",
      "summary": "Test 'external-only evidence cannot satisfy existing project facts' uses evidence with scope='external' and source='external_web', then asserts readiness.prd_ready === false and blocker code === 'PROJECT_FACT_REQUIRES_PROJECT_EVIDENCE'.",
      "why": "This test validates the blocking behavior when external-only evidence is presented for existing-project claims."
    },
    {
      "path": "__tests__/demand-router.test.ts",
      "line": "387-427",
      "scope": "project",
      "source": "project_test",
      "summary": "Test 'non-missing status notes in missing arrays do not block readiness' includes missing: ['No missing data identified.'] then asserts blockers array is empty, confirming sanitizeMissing() filters status notes.",
      "why": "This test confirms the missing field is for actionable gaps only, not status messages."
    },
    {
      "path": "src/demand/router.ts",
      "line": "52-75",
      "scope": "project",
      "source": "project_code",
      "summary": "DEMAND_EVIDENCE_AGENT_PROTOCOLS defines explorer, cross_checker, and verifier protocols with writes_code=false, result_schema=DEMAND_EVIDENCE_RESULT_SCHEMA, and cross_checker.required_when='evidence_policy === cross_check or claim risk is high'.",
      "why": "This establishes the three-agent cross-check evidence policy architecture."
    },
    {
      "path": "__tests__/demand-evidence-dispatch.test.ts",
      "line": "378-411",
      "scope": "project",
      "source": "project_test",
      "summary": "Test 'boundary blocks project file mutation even when agent results pass' uses spawnProviderPrompt callback to write 'unexpected-project-write.txt' then asserts result.status === 'blocked', boundary.project_mutation === 'violated', and blocker code === 'BOUNDARY_PROJECT_MUTATION'.",
      "why": "This test validates the boundary audit detects target-project mutations even when evidence agents pass."
    },
    {
      "path": "src/demand/router.ts",
      "line": "96-108",
      "scope": "project",
      "source": "project_code",
      "summary": "isNonMissingStatusItem() filters patterns like 'no missing', 'all complete', 'no conflicts' from missing arrays. actionableMissingItems() applies this filter to return only actionable gaps.",
      "why": "This ensures missing arrays contain only actual gaps, not status notes."
    },
    {
      "path": "src/demand/evidence-dispatch.ts",
      "line": "147-153",
      "scope": "project",
      "source": "project_code",
      "summary": "sanitizeMissing() filters out items matching isNonMissingStatusItem() patterns, confirming missing field enforcement in the evidence-dispatch module.",
      "why": "This duplicate implementation confirms missing field governance across modules."
    }
  ],
  "assumptions": [],
  "risks": [
    "If evidence agents misclassify source field (e.g., mark project_code as external_web), scope normalization could bypass project-evidence requirements.",
    "If scope field is omitted entirely, evidenceRecordScope() defaults to 'unknown', which could allow non-project evidence to pass validation for project facts."
  ],
  "missing": [
    "Verification of whether scope normalization logic correctly handles all edge cases in source field values (e.g., custom source types not explicitly listed)."
  ],
  "recommendation": "proceed",
  "result": {
    "verdict": "pass",
    "notes": "All 10 evidence records from explorer independently verified. Schema definition, scope normalization, blocking logic, agent protocols, test coverage, and boundary enforcement all confirmed. Four evidence scopes (project, external, user, unknown) are explicitly defined, enforced at runtime, and tested. External research is clearly separated from project facts."
  }
}
