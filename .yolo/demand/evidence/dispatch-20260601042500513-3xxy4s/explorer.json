{
  "schema_version": "1.0",
  "schema": "yolo.demand.evidence_result.v1",
  "role": "explorer",
  "status": "completed",
  "completed": true,
  "claim": "YOLO demand evidence dispatch implements a fail-closed JSON repair strategy: minor recoverable formatting issues from Claude are automatically repaired, while unrecoverable JSON returns explicit EVIDENCE_AGENT_INVALID_JSON blockers, keeping PRD readiness blocked until valid evidence is received.",
  "confidence": "high",
  "evidence": [
    {
      "path": "src/demand/evidence-dispatch.ts",
      "line": "330-346",
      "scope": "project",
      "source": "project_code",
      "summary": "parseJsonCandidate implements repair-on-fail: first attempts direct JSON.parse, then applies regex repairs for common drift patterns (line ranges, escaping, key quotes, trailing commas), and returns {parsed, repaired: true} on successful repair",
      "why": "This is the core repair logic that handles minor Claude JSON formatting drift."
    },
    {
      "path": "src/demand/evidence-dispatch.ts",
      "line": "334-338",
      "scope": "project",
      "source": "project_code",
      "summary": "Repair regex chain handles: line number ranges (42-88), missing backslashes, empty key names (\" \"summary\"), and trailing commas before brackets/braces",
      "why": "These are the specific drift patterns the repair logic targets as recoverable."
    },
    {
      "path": "src/demand/evidence-dispatch.ts",
      "line": "370-396",
      "scope": "project",
      "source": "project_code",
      "summary": "normalizeAgentResult returns a structured error with error_code EVIDENCE_AGENT_INVALID_JSON when parsed is null or providerRun.success is false, including the parse error and truncated output in the result",
      "why": "This is the fail-closed behavior that produces explicit blockers for unrecoverable JSON."
    },
    {
      "path": "src/demand/evidence-dispatch.ts",
      "line": "566-568",
      "scope": "project",
      "source": "project_code",
      "summary": "The runtime records json_repaired: true in provider_runs when parsedOutput.repaired is true, tracking which agent outputs needed repair",
      "why": "This provides traceability for which agents had drift that required repair."
    },
    {
      "path": "src/demand/evidence-dispatch.ts",
      "line": "574-604",
      "scope": "project",
      "source": "project_code",
      "summary": "After agent execution, boundary violations and readiness blockers are merged, and final status is set to blocked if prd_ready is false with code DEMAND_EVIDENCE_DISPATCH_BLOCKED",
      "why": "This ensures PRD readiness stays blocked when evidence agents return invalid JSON or other blockers."
    },
    {
      "path": "__tests__/demand-evidence-dispatch.test.ts",
      "line": "535-571",
      "scope": "project",
      "source": "project_test",
      "summary": "Test repairs minor Claude JSON key drift and records the repair injects common drift patterns (line ranges, backslash escapes, quoted key names) in fenced JSON and asserts status passes and json_repaired is true",
      "why": "This test demonstrates the repair logic works for recoverable drift scenarios."
    },
    {
      "path": "__tests__/demand-evidence-dispatch.test.ts",
      "line": "573-603",
      "scope": "project",
      "source": "project_test",
      "summary": "Test invalid agent JSON fails closed with explicit blocker code sends stdout: 'not json' and asserts status is blocked, agent results have error_code EVIDENCE_AGENT_INVALID_JSON, and readiness blockers include the same code",
      "why": "This test verifies the fail-closed behavior for unrecoverable JSON produces explicit blockers."
    },
    {
      "path": "src/demand/evidence-dispatch.ts",
      "line": "407-419",
      "scope": "project",
      "source": "project_code",
      "summary": "normalizeAgentResult also validates evidence scope requirements via evidenceScopeErrors and blocks with EVIDENCE_SCOPE_REQUIRED if evidence records lack scope field",
      "why": "This demonstrates the dispatch enforces schema validation beyond just JSON parsing."
    }
  ],
  "assumptions": [
    "The regex repair patterns in parseJsonCandidate cover the majority of Claude output drift; this is inferred from test coverage but not proven for all possible drift variants."
  ],
  "risks": [
    "If Claude introduces a new JSON drift pattern not covered by the existing repair regex chain, valid agent output could incorrectly fail with EVIDENCE_AGENT_INVALID_JSON.",
    "The repair logic is stateless and does not learn from new drift patterns over time."
  ],
  "missing": [],
  "recommendation": "proceed",
  "result": {
    "verdict": "pass",
    "notes": "Evidence confirms the implementation matches the stated behavior: recoverable drift is repaired and tracked, unrecoverable JSON produces explicit blockers, and PRD readiness remains blocked until valid evidence is received."
  }
}
