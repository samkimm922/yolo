{
  "schema_version": "1.0",
  "schema": "yolo.demand.evidence_result.v1",
  "role": "explorer",
  "status": "completed",
  "completed": true,
  "claim": "YOLO demand evidence dispatch implements minor JSON drift repair with parseJsonCandidate and fails closed with EVIDENCE_AGENT_INVALID_JSON error code when JSON cannot be recovered.",
  "confidence": "high",
  "evidence": [
    {
      "path": "src/demand/evidence-dispatch.ts",
      "line": "330-346",
      "scope": "project",
      "source": "project_code",
      "summary": "parseJsonCandidate attempts direct JSON.parse, then applies four repair patterns on failure: line range stringification, backslash escaping, double-quote key normalization, and trailing comma removal. Returns {parsed, repaired: boolean, error: string}.",
      "why": "This is the core repair logic for recoverable Claude JSON key drift."
    },
    {
      "path": "src/demand/evidence-dispatch.ts",
      "line": "348-368",
      "scope": "project",
      "source": "project_code",
      "summary": "extractJsonObject tries three extraction strategies in sequence: direct parse, fenced code block extraction (```json), and slicing from first { to last }. Each attempt invokes parseJsonCandidate with repair enabled.",
      "why": "This ensures JSON is extracted regardless of Claude formatting, with repair attempted at each stage."
    },
    {
      "path": "src/demand/evidence-dispatch.ts",
      "line": "370-396",
      "scope": "project",
      "source": "project_code",
      "summary": "normalizeAgentResult checks if parsing succeeded. If !parsed, sets errorCode to EVIDENCE_AGENT_INVALID_JSON, returns a blocked result with the error code in result.error_code, and records parseError in the missing array.",
      "why": "This is the fail-closed behavior that blocks PRD readiness on unrecoverable JSON."
    },
    {
      "path": "src/demand/evidence-dispatch.ts",
      "line": "372",
      "scope": "project",
      "source": "project_code",
      "summary": "const errorCode = !parsed ? 'EVIDENCE_AGENT_INVALID_JSON' : 'EVIDENCE_AGENT_PROVIDER_FAILED';",
      "why": "This conditional distinguishes JSON parse failures from provider runtime failures."
    },
    {
      "path": "__tests__/demand-evidence-dispatch.test.ts",
      "line": "535-571",
      "scope": "project",
      "source": "project_test",
      "summary": "Test repairs minor Claude JSON key drift simulates line range as 42-88 instead of string, escaped backslashes, and double-quote key drift. Asserts result.status is pass and result.provider_runs has json_repaired === true.",
      "why": "This proves that repair logic works and does not block dispatch when drift is recoverable."
    },
    {
      "path": "__tests__/demand-evidence-dispatch.test.ts",
      "line": "573-603",
      "scope": "project",
      "source": "project_test",
      "summary": "Test invalid agent JSON fails closed with explicit blocker code provides stdout: 'not json'. Asserts result.status is blocked, agent_results have error_code EVIDENCE_AGENT_INVALID_JSON, and readiness.blockers includes code EVIDENCE_AGENT_INVALID_JSON.",
      "why": "This proves that unrecoverable JSON blocks dispatch and PRD readiness with explicit error code."
    },
    {
      "path": "src/demand/evidence-dispatch.ts",
      "line": "542-543",
      "scope": "project",
      "source": "project_code",
      "summary": "const parsedOutput = extractJsonObject(providerRun.stdout); const agentResult = normalizeAgentResult({action, providerRun, parsed: parsedOutput.parsed, parseError: parsedOutput.error});",
      "why": "This is the integration point where provider stdout is parsed, repaired if possible, and normalized into agent results."
    },
    {
      "path": "src/demand/evidence-dispatch.ts",
      "line": "558",
      "scope": "project",
      "source": "project_code",
      "summary": "json_repaired: parsedOutput.repaired === true is recorded in provider_runs array",
      "why": "This provides an audit trail of which agent outputs required JSON repair."
    },
    {
      "path": "src/demand/evidence-dispatch.ts",
      "line": "598-604",
      "scope": "project",
      "source": "project_code",
      "summary": "readiness = inspectDemandPrdReadiness is called with evidence_results. If boundary violations occur, blockers are added and prd_ready becomes false. Final result.status and result.code depend on readiness.prd_ready.",
      "why": "This shows how agent errors propagate to PRD readiness blocking."
    }
  ],
  "assumptions": [],
  "risks": [
    "If parseJsonCandidate repair patterns do not cover new Claude drift variants, valid JSON could be incorrectly marked as EVIDENCE_AGENT_INVALID_JSON.",
    "If extractJsonObject slicing strategy matches too broadly, it could extract partial JSON that passes parse but loses context."
  ],
  "missing": [],
  "recommendation": "proceed",
  "result": {
    "verdict": "pass",
    "notes": "Evidence confirms implementation matches demand: minor drift is repaired via parseJsonCandidate with four regex patterns, unrecoverable JSON receives EVIDENCE_AGENT_INVALID_JSON error code, and PRD readiness stays blocked. Test coverage validates both repair and fail-closed paths."
  }
}
